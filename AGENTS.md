# Repository Instructions

- VBA `.bas` files must be saved as UTF-8.
- VBA `.bas` files must use CRLF line endings.
- After editing `.bas` files, verify they can be read back as UTF-8 and contain no lone LF line endings.
